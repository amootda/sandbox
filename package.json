{
  "name": "sandbox",
  "version": "1.0.0",
  "description": "Sandbox for experimental projects",
  "private": true,
  "scripts": {
    "dev:progressBar": "concurrently \"pnpm --filter server dev\" \"pnpm --filter client dev\"",
    "dev:progressBar:server": "pnpm --filter server dev",
    "dev:progressBar:client": "pnpm --filter client dev",
    "install:all": "pnpm install"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/amootda/sandbox.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/amootda/sandbox/issues"
  },
  "homepage": "https://github.com/amootda/sandbox#readme",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
